sudo: required

services:
  - docker

language: python

python:
  - "3.6"

install:
  - docker build -t dockerfile .

script:
  - docker run -d -p 127.0.0.1:80:4567 --name bagpipe_test dockerfile pytest
